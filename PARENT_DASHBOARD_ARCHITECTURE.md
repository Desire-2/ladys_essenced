# Parent Dashboard - Architecture & Visual Guide

## 🏗️ System Architecture

### High-Level Overview
```
┌─────────────────────────────────────────────────────────────────┐
│                        Parent Dashboard                          │
│                    (http://localhost:3000/dashboard/parent)      │
└─────────────────────────────────────────────────────────────────┘
                              │
                    ┌─────────┴──────────┐
                    │                    │
            ┌───────▼────────┐  ┌────────▼───────┐
            │  React Context │  │  Browser Local │
            │    (Global)    │  │    Storage     │
            └────────────────┘  └────────────────┘
                    │                    │
        ┌───────────┴────────────────────┴────────────┐
        │                                             │
    ┌───▼────────────────────────┐  ┌────────────────▼─────┐
    │     Component Hierarchy    │  │   API Client Layer   │
    │  (React Components)        │  │  (HTTP Requests)     │
    └────────────────────────────┘  └──────────────────────┘
        │                                     │
        └─────────────────┬───────────────────┘
                          │
            ┌─────────────▼──────────────┐
            │   Flask Backend API        │
            │  (http://localhost:5000)   │
            └────────────────────────────┘
                          │
            ┌─────────────▼──────────────┐
            │  PostgreSQL Database       │
            │  (Users, Children, Data)   │
            └────────────────────────────┘
```

## 📦 Component Hierarchy

```
ParentDashboard (Main Page)
│
├─ Header Section
│  ├─ Welcome Message
│  ├─ Quick Stats Card
│  │  ├─ Children Count
│  │  ├─ Active Monitoring
│  │  └─ Account Status
│  └─ Logout Button
│
├─ Tab Navigation
│  ├─ Overview Tab
│  ├─ Add Child Tab
│  └─ Monitor Tab
│
├─ Tab Content - Overview
│  ├─ ChildrenList
│  │  ├─ Child Card (Repeating)
│  │  │  ├─ Child Name
│  │  │  ├─ Age Display
│  │  │  ├─ Relationship Badge
│  │  │  └─ Action Buttons
│  │  └─ Empty State
│  │
│  └─ Quick Actions Sidebar
│     ├─ Features Card
│     └─ Quick Action Buttons
│
├─ Tab Content - Add Child
│  └─ AddChildForm
│     ├─ Name Input
│     ├─ DOB Input
│     ├─ Relationship Select
│     ├─ Password Input
│     ├─ Form Validation
│     ├─ Error/Success Messages
│     └─ Submit Button
│
└─ Tab Content - Monitor
   └─ ChildMonitoring
      ├─ Tab Navigation
      │  ├─ Cycle Tab
      │  ├─ Meals Tab
      │  └─ Appointments Tab
      │
      ├─ Cycle Content
      │  ├─ Stats Summary
      │  ├─ Cycle Table
      │  └─ Empty State
      │
      ├─ Meals Content
      │  ├─ Meal Summary
      │  ├─ Meals List
      │  └─ Empty State
      │
      └─ Appointments Content
         ├─ Appointments List
         └─ Empty State
```

## 🔄 Data Flow Architecture

### Loading Children Data Flow
```
User visits /dashboard/parent
        │
        ▼
useEffect triggers
        │
        ▼
fetchChildren() called
        │
        ▼
ParentAPI.getChildren()
        │
        ▼
HTTP GET /api/parents/children
        │
        ▼
Backend validation (JWT, role check)
        │
        ▼
Query: SELECT * FROM parent_children WHERE parent_id = ?
        │
        ▼
Return child data
        │
        ▼
setChildrenList(data)
        │
        ▼
ChildrenList component re-renders
        │
        ▼
Display children cards
```

### Selecting Child & Fetching Data
```
User clicks on child card
        │
        ▼
setSelectedChild(childId)
        │
        ▼
useEffect detects activeTab change
        │
        ▼
fetchChildCycleLogs(childId)
fetchChildMealLogs(childId)
fetchChildAppointments(childId)
        │
        ▼
Parallel API calls to backend
        │
        ├─ GET /api/parents/children/{id}/cycle-logs
        ├─ GET /api/parents/children/{id}/meal-logs
        └─ GET /api/parents/children/{id}/appointments
        │
        ▼
Backend processes each request
        │
        ▼
Update childCycleLogs, childMealLogs, childAppointments
        │
        ▼
ChildMonitoring component displays data
```

### Adding New Child Flow
```
User fills out form and submits
        │
        ▼
handleChildFormSubmit
        │
        ├─ Validate form data
        │
        ▼ (if valid)
addChild({name, dob, relationship, password})
        │
        ▼
ParentAPI.addChild(childData)
        │
        ▼
HTTP POST /api/parents/children
        │
        ├─ Create User record
        ├─ Create Adolescent record
        ├─ Create ParentChild relationship
        │
        ▼
Return new child data
        │
        ▼
setChildrenList([...childrenList, newChild])
        │
        ▼
ChildrenList re-renders with new child
        │
        ▼
Show success message
```

## 🎨 UI Layout Structure

### Desktop View (> 992px)
```
┌─────────────────────────────────────────────────────────┐
│  Header (Gradient Background)                           │
│  ┌────────────────┐  ┌──────────────────────────────┐  │
│  │  Title         │  │  Stat Cards                  │  │
│  │  + Logout      │  │  [Stats] [Stats] [Stats]     │  │
│  └────────────────┘  └──────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────┐
│  Tabs: [Overview] [Add Child] [Monitor]                 │
└─────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────┐
│  Left Content (70%)         │  Right Sidebar (30%)       │
│  ┌────────────────────────┐ │ ┌──────────────────────┐  │
│  │ Children Cards Grid    │ │ │ Quick Actions Card   │  │
│  │ ┌──────┐ ┌──────┐    │ │ │ ┌──────────────────┐ │  │
│  │ │Child │ │Child │    │ │ │ │ Features List    │ │  │
│  │ └──────┘ └──────┘    │ │ │ └──────────────────┘ │  │
│  │                       │ │ └──────────────────────┘  │
│  └────────────────────────┘ │                          │
└─────────────────────────────────────────────────────────┘
```

### Tablet View (576px - 992px)
```
┌──────────────────────────────┐
│  Header (Stacked)            │
│  Title                       │
│  Stats Cards                 │
│  Logout                      │
└──────────────────────────────┘
┌──────────────────────────────┐
│  Tabs                        │
└──────────────────────────────┘
┌──────────────────────────────┐
│  Content (Single Column)     │
│  ┌────────────────────────┐  │
│  │ Children Card         │  │
│  └────────────────────────┘  │
│  ┌────────────────────────┐  │
│  │ Quick Actions Card     │  │
│  └────────────────────────┘  │
└──────────────────────────────┘
```

### Mobile View (< 576px)
```
┌──────────────┐
│ Header       │
│ Title        │
│ Stats        │
│ Logout       │
└──────────────┘
┌──────────────┐
│ Tabs         │
└──────────────┘
┌──────────────┐
│ Children     │
│ Cards        │
│ (vertical)   │
└──────────────┘
┌──────────────┐
│ Quick        │
│ Actions      │
└──────────────┘
```

## 🧠 State Management

### ParentContext State
```
ParentContext
│
├─ Children Management
│  ├─ childrenList: Child[]
│  ├─ selectedChild: number | null
│  ├─ childrenLoading: boolean
│  ├─ childrenError: string | null
│  │
│  ├─ Methods:
│  ├─ fetchChildren()
│  ├─ fetchChild(id)
│  ├─ addChild(data)
│  ├─ updateChild(id, data)
│  └─ deleteChild(id)
│
├─ Child Data Caching
│  ├─ childCycleLogs: { [childId]: CycleLogs }
│  ├─ childMealLogs: { [childId]: MealLogs }
│  ├─ childAppointments: { [childId]: Appointments }
│  │
│  ├─ Methods:
│  ├─ fetchChildCycleLogs(id)
│  ├─ fetchChildMealLogs(id)
│  ├─ fetchChildAppointments(id)
│  ├─ getChildCycleData(id)
│  ├─ getChildMealData(id)
│  └─ getChildAppointmentData(id)
│
└─ Loading & Error States
   ├─ loadingStates: { [key]: boolean }
   ├─ errors: { [key]: string }
   ├─ getLoadingState(key)
   └─ getError(key)
```

## 🔐 Security Architecture

### Authentication Flow
```
User Login
    │
    ▼
Store JWT token (localStorage)
    │
    ▼
Store user_type, user_id
    │
    ▼
Redirect to /dashboard/parent
    │
    ▼
Check: user exists? ✓
Check: user_type === 'parent'? ✓
    │
    ▼
Load dashboard
    │
    ├─ Every API request includes JWT
    │
    └─ Backend validates:
       ├─ JWT is valid?
       ├─ User is parent?
       └─ Parent owns child?
```

### Parent-Child Authorization
```
API Request: GET /api/parents/children/123/cycle-logs
    │
    ▼
Extract parent_id from JWT
    │
    ▼
Query: SELECT * FROM parent_children 
       WHERE parent_id=? AND adolescent_id=123
    │
    ├─ Found? ✓ Return data
    └─ Not found? ✗ Return 403 Forbidden
```

## 📊 Database Relationships

### Entity Relationship Diagram
```
┌─────────────────┐
│     Users       │
├─────────────────┤
│ id (PK)         │
│ name            │
│ email           │
│ phone_number    │
│ user_type       │
│ password_hash   │
└─────────────────┘
      │ 1..N
      │ (user_id)
      │
      ├──────────────┬────────────────┬──────────────┐
      │              │                │              │
      ▼ 1..N    ▼ 1..N           ▼ 1..N      ▼ 1..N
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ Parents  │ │Adolescent│ │  Admin   │ │ Writers  │
├──────────┤ ├──────────┤ ├──────────┤ ├──────────┤
│ id       │ │id        │ │id        │ │id        │
│ user_id  │ │user_id   │ │user_id   │ │user_id   │
│ (FK)     │ │(FK)      │ │(FK)      │ │(FK)      │
└──────────┘ └──────────┘ └──────────┘ └──────────┘
      │            △
      │ 1..N       │
      │ (parent_id)│ 1..N
      │            │ (adolescent_id)
      └────────────┤
          ┌────────────────────┐
          │  ParentChildren    │
          ├────────────────────┤
          │ id (PK)            │
          │ parent_id (FK)     │
          │ adolescent_id (FK) │
          │ relationship_type  │
          └────────────────────┘
                   │ 1..N
                   │ (adolescent_id)
                   │
        ┌──────────┴────────────────┬──────────┐
        │                           │          │
        ▼ 1..N               ▼ 1..N    ▼ 1..N
┌─────────────────┐ ┌─────────────┐ ┌────────────────┐
│   CycleLogs     │ │  MealLogs   │ │  Appointments  │
├─────────────────┤ ├─────────────┤ ├────────────────┤
│ id (PK)         │ │ id (PK)     │ │ id (PK)        │
│ user_id (FK)    │ │ user_id(FK) │ │ user_id (FK)   │
│ start_date      │ │ meal_time   │ │ appointment_d. │
│ end_date        │ │ meal_type   │ │ issue          │
│ flow_intensity  │ │ description │ │ status         │
│ symptoms        │ │ calories    │ │ provider_id    │
└─────────────────┘ └─────────────┘ └────────────────┘
```

## 🔄 API Endpoint Mapping

### Parent Endpoints
```
GET /api/parents/children
├─ Requires: JWT token, parent role
├─ Returns: Array<Child>
└─ Used by: fetchChildren()

GET /api/parents/children/{id}
├─ Requires: JWT token, owns child
├─ Returns: Child
└─ Used by: fetchChild()

POST /api/parents/children
├─ Requires: JWT token, parent role, child data
├─ Returns: { message, child }
└─ Used by: addChild()

PUT /api/parents/children/{id}
├─ Requires: JWT token, owns child, update data
├─ Returns: { message }
└─ Used by: updateChild()

DELETE /api/parents/children/{id}
├─ Requires: JWT token, owns child
├─ Returns: { message }
└─ Used by: deleteChild()

GET /api/parents/children/{id}/cycle-logs
├─ Requires: JWT token, owns child
├─ Pagination: ?page=1&per_page=10
├─ Returns: { items, total, pages }
└─ Used by: fetchChildCycleLogs()

GET /api/parents/children/{id}/meal-logs
├─ Requires: JWT token, owns child
├─ Pagination: ?page=1&per_page=10
├─ Returns: { items, total, pages }
└─ Used by: fetchChildMealLogs()

GET /api/parents/children/{id}/appointments
├─ Requires: JWT token, owns child
├─ Pagination: ?page=1&per_page=10
├─ Returns: { items, total, pages }
└─ Used by: fetchChildAppointments()
```

## 📈 Performance Optimization Strategy

### Caching Layer
```
Memory Cache (Context)
    │
    ├─ Child data (childrenList)
    ├─ Cycle logs (childCycleLogs)
    ├─ Meal logs (childMealLogs)
    └─ Appointments (childAppointments)
         │
         └─ Avoid redundant API calls
```

### Loading Strategy
```
Lazy Loading:
- Load children on dashboard mount
- Load child data only when selected
- Load specific tab data on tab click

Pagination:
- Display 10 items per page
- Load more on scroll (future)
- Reduce initial payload
```

### State Updates
```
Optimized Re-renders:
- Use Context memoization
- Only relevant components update
- Avoid unnecessary re-renders
- Use React.memo where needed
```

## 🎯 Use Case Diagrams

### Add Child Use Case
```
┌─────────────────────────────────────┐
│         Parent Dashboard            │
└─────────────────────────────────────┘
            │
            ▼
    ┌───────────────────┐
    │  Click Add Child  │
    └───────────────────┘
            │
            ▼
    ┌───────────────────────────┐
    │  Navigate to Add Tab      │
    └───────────────────────────┘
            │
            ▼
    ┌───────────────────────────┐
    │  Fill Child Form          │
    │  - Name                   │
    │  - DOB                    │
    │  - Relationship           │
    │  - Password               │
    └───────────────────────────┘
            │
            ▼
    ┌───────────────────────────┐
    │  Validate Form            │
    │  - Check required fields  │
    │  - Validate dates         │
    │  - Check password strength│
    └───────────────────────────┘
            │
    ├─ Valid?  │  Invalid?
    │          │
    ▼          ▼
   Yes        Show
    │         Error
    │         │
    ▼         └─ Return to form
Submit
    │
    ▼
API Call: POST /api/parents/children
    │
    ├─ Success ▼
    │    Create: User + Adolescent + ParentChild
    │    │
    │    ▼
    │  Return child data
    │    │
    │    ▼
    │  Update context
    │    │
    │    ▼
    │  Show success message
    │    │
    │    ▼
    │  Child appears in list
    │
    └─ Error
        │
        ▼
      Show error message
        │
        ▼
      Return to form
```

### Monitor Child Use Case
```
┌─────────────────────────────┐
│  Parent Dashboard Overview  │
└─────────────────────────────┘
            │
            ▼
    ┌──────────────────┐
    │  Children List   │
    └──────────────────┘
            │
            ▼
    ┌──────────────────┐
    │  Click on Child  │
    └──────────────────┘
            │
            ▼
    ┌──────────────────────┐
    │  setSelectedChild()   │
    └──────────────────────┘
            │
            ▼
    ┌──────────────────────────────┐
    │  Click Monitor Tab           │
    └──────────────────────────────┘
            │
            ▼
    ┌──────────────────────────────┐
    │  Load monitoring data:       │
    │  - fetchChildCycleLogs()    │
    │  - fetchChildMealLogs()     │
    │  - fetchChildAppointments() │
    └──────────────────────────────┘
            │
            ▼
    ┌──────────────────────────────┐
    │  Display ChildMonitoring     │
    │  Component with tabs         │
    └──────────────────────────────┘
            │
    ┌───────┼───────┬─────────┐
    │       │       │         │
    ▼       ▼       ▼         ▼
  Cycle  Meals  Appointments  Select Tab
  Data   Data   Data          │
    │       │       │         ▼
    └───────┴───────┴─ Display relevant tab
```

## 🌐 Full System Integration

```
┌────────────────────────────────────────────────────────────┐
│                    Frontend (React/Next.js)                 │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                Parent Dashboard                         │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │ ParentContext (State Management)                 │  │ │
│  │  │ - Children list, Selected child, Cached data    │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │ Components                                       │  │ │
│  │  │ - ChildrenList, AddChildForm, ChildMonitoring  │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ API Client Layer (HTTP Requests)                       │ │
│  └────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
         │
         │ HTTP/HTTPS
         ▼
┌────────────────────────────────────────────────────────────┐
│              Backend (Flask/Python)                        │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ API Routes (/api/parents/*)                           │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Auth Middleware                                        │ │
│  │ - JWT validation, Role check                          │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Business Logic                                         │ │
│  │ - Parent operations, Child management                 │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Database Models                                        │ │
│  │ - User, Parent, Child, Data                           │ │
│  └────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
         │
         │ SQL Queries
         ▼
┌────────────────────────────────────────────────────────────┐
│           Database (PostgreSQL)                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Tables: users, parents, adolescents, parent_children,  │ │
│  │ cycle_logs, meal_logs, appointments, notifications     │ │
│  └────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

---

**This architecture ensures**:
- ✅ Clean separation of concerns
- ✅ Scalable data management
- ✅ Secure parent-child relationships
- ✅ Responsive user experience
- ✅ Efficient state management
- ✅ Proper error handling

**Version**: 1.0.0  
**Last Updated**: November 5, 2025
